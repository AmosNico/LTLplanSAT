name:                LTLplanSAT
version:             0.1.0.0
github:              "githubuser/LTLplanSAT"
license:             BSD-3-Clause
author:              "Amos Nicodemus"
maintainer:          "nicodemusamos@gmail.com"
copyright:           "2024 Amos Nicodemus"

extra-source-files:
- README.md
- CHANGELOG.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/LTLplanSAT#readme>

dependencies:
- base >= 4.7 && < 5
- bytestring >= 0.11.5.3
- vector >= 0.13.1.0
- containers >= 0.6.7

ghc-options:
- -Wall
- -Wcompat
- -Widentities
- -Wincomplete-record-updates
- -Wincomplete-uni-patterns
- -Wmissing-export-lists
- -Wmissing-home-modules
- -Wpartial-fields
- -Wredundant-constraints

library:
  source-dirs: src

executables:
  LTLplanSAT-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - LTLplanSAT

tests:
  LTLplanSAT-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - LTLplanSAT
